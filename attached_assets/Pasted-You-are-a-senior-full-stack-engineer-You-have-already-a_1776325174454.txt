You are a senior full-stack engineer. You have already audited this codebase and 
produced a full findings report. That report is reproduced below as your reference. 
Now you will implement fixes in strict priority order.

Your previous audit found:
[[AUTH ISSUES]
Critical overarching finding: The auth migration described in the prompt — replacing Clerk with custom JWT + bcrypt — has not happened at all. There is no lib/auth.ts. The custom auth system does not exist in this codebase. Every single auth touchpoint is still 100% Clerk. The issues below are reality as the code stands today.

1. middleware.ts — still wraps everything in clerkMiddleware
The entire middleware is clerkMiddleware(async (auth, req) => ...) with Clerk's createRouteMatcher. Auth checking via const { userId } = await auth() on line 83 is Clerk's auth function. No JWT verification is happening. If Clerk keys are removed, every protected route becomes inaccessible immediately.

2. app/layout.tsx — root layout wraps the entire app in <ClerkProvider>
Every page in the app depends on Clerk's React context. Removing Clerk breaks useAuth, useUser, UserButton, SignOutButton everywhere they are used across the component tree.

3. lib/admin-auth.ts — requireAdminClerkId() calls Clerk auth() and getUserRole()
This function is callable from server components and returns a Clerk userId. If Clerk is removed, callers (wherever this is invoked) get null and silently fail.

4. lib/app-user.ts — syncAppUserFromClerk() inserts clerkId into the users table
Line: .values({ clerkId: userId, email, name, role: 'pro' }) and .onConflictDoUpdate({ target: users.clerkId, ... }). If the users.clerkId column is dropped from the schema (as described in the migration intent), this file breaks entirely and takes down all user upsert logic with it.

5. lib/platform-role.ts — isPlatformSuperAdmin() calls clerkClient() to check user metadata
The super-admin check is based on Clerk's publicMetadata.platformRole === 'super_admin'. There is no equivalent concept in a custom JWT system yet. The platform/ layout redirects all non-super-admins away — if this check breaks, the platform section becomes either always-open or always-locked.

6. lib/role.ts — getUserRole() reads from Clerk JWT session claims and Clerk currentUser()
The role system reads Clerk sessionClaims.metadata.role and falls back to user.publicMetadata.role. No custom JWT equivalent exists.

7. lib/tenant-api.ts — requireTenantMemberApi() calls Clerk auth() for userId
Every API route that calls requireTenantMemberApi() or requireTenantAdminApi() uses Clerk's auth() to get the user. This is the single auth entrypoint for: leads, notes, reminders, documents, kanban, import, notifications, team management, and role requests — all API routes in the app.

8. lib/tenant-server.ts — requireTenantSession() calls Clerk auth() for userId
All server-rendered admin and pro pages use this function. The same dependency propagates to every page layout.

9. lib/tenant-access.ts — resolveTenantAccess() fully depends on Clerk
Calls isPlatformSuperAdminUserId() (Clerk metadata check) and syncTenantMembership() (Clerk org membership lookup). There is no non-Clerk path for resolving access.

10. lib/tenant-membership.ts — syncTenantMembership() calls Clerk org API
This function calls client.users.getOrganizationMembershipList() to check if a user belongs to a Clerk org. It also inserts using clerkId as the conflict target. Removing Clerk breaks tenant membership resolution for every normal user.

11. app/page.tsx — home page calls clerkClient() to get org memberships
Authenticated users hitting / are redirected by checking their Clerk org memberships. If Clerk is removed and the user has no memberships in DB, they hit the orgIds.length === 0 branch and are redirected to /request-role regardless of whether they're legitimately a member.

12. app/sign-in/ and app/sign-up/ — render Clerk's hosted <SignIn> and <SignUp> components
No custom login or registration pages exist. If Clerk keys are absent, these pages crash or show blank Clerk widgets with no functionality.

13. app/dashboard/page.tsx — dead test page that uses auth() and <UserButton>
This page is not linked from anywhere in the app. It exists as a leftover test scaffold and imports currentUser + UserButton from Clerk. It should be deleted.

14. app/platform/actions.ts — createWorkspaceAction() creates a Clerk Organization before inserting a tenant
Workspace creation is a two-step process: first create a Clerk org, then insert into DB with clerkOrgId. If Clerk is removed, tenant creation is fully broken. The first admin also receives a Clerk invitation email — there is no custom invitation flow.

15. app/request-role/page.tsx — uses clerkClient() to check org memberships and <SignOutButton>
If a user has org memberships in Clerk they get redirected away. If not, they see the request form. The form posts to /api/role-requests which also stores clerkId in the roleRequests table.

16. app/no-role/page.tsx — uses auth() to redirect signed-in users
Minor: a signed-in user (determined by Clerk auth()) is redirected to /request-role. If Clerk is absent, all signed-in users see the "not signed in" fallback text even when they are signed in.

17. components/auth-toast-wrapper.tsx — uses Clerk's useAuth and useUser React hooks
Rendered in the root layout. If Clerk context is missing, this throws a React error that crashes every page.

18. components/tenant/TenantOrgSync.tsx — uses useAuth and useOrganizationList from Clerk
This component calls setActive({ organization: clerkOrgId }) to sync the user's active Clerk org. It is rendered on every page inside app/t/[tenantSlug]/layout.tsx. It relies entirely on Clerk and references tenant.clerkOrgId.

19. app/api/admin/role-requests/[id]/route.ts — approving a request calls client.organizations.createOrganizationMembership()
Approving an access request adds the user to a Clerk org. Without Clerk, approval grants no real access since syncTenantMembership also checks Clerk.

20. components/admin/admin-header.tsx and components/admin/admin-sidebar.tsx — render <UserButton> and <SignOutButton>
Both are Clerk components. The user's avatar, name, and sign-out button in every admin interface depend on Clerk.

21. app/t/[tenantSlug]/admin/team/page.tsx — imports clerkClient (though doesn't currently use it in the snippet)
The import is present. Any future calls would depend on Clerk.

22. app/t/[tenantSlug]/pro/overview/page.tsx — renders <UserButton> from Clerk
The agent overview page shows the Clerk user button. Without Clerk, this breaks the UI.

23. db/schema.ts — users.clerkId, tenants.clerkOrgId, roleRequests.clerkId, roleRequests.reviewedByClerkId columns are all still present
The schema is completely unchanged from the pre-migration state. The claimed migration (dropping Clerk columns, adding password + avatarUrl) has not occurred.

24. Dead-end auth flow — no role + no Clerk org = stuck at /request-role indefinitely
When a user signs up and is neither in a Clerk org nor a super admin, they land on /request-role. Their request goes to the DB with a clerkId. Approval adds them to a Clerk org. There is no fallback: if a super admin approves in the DB without also going through Clerk, syncTenantMembership will still find them absent from the org and return null, so they'll be redirected to /no-access forever.

25. Invite flow failure point — inviteWorkspaceUserAction and createWorkspaceAction both depend on workspaceOrigin() resolving correctly
workspaceOrigin() uses NEXT_PUBLIC_ROOT_DOMAIN. If this env var is not set or doesn't match the actual Replit domain, the Clerk invitation redirect URL will point to the wrong place. Users who click the invite email land on an unrelated or broken URL.

[CODE ISSUES]
1. db/schema.ts — leadDocumentChecklist.required and isSubmitted are text columns storing 'true'/'false' strings
This means SQL comparisons like WHERE is_submitted = true won't work. Every consumer of these fields must coerce the string, creating bugs. These should be boolean columns.

2. db/schema.ts — notifications.read is a text column with default 'false'
Same anti-pattern as above. String 'false' is truthy in JavaScript, so if (notification.read) is always true unless explicitly checked against the string 'false'.

3. scripts/fix-clerkid.ts, fix-pro-clerkid.ts, fix-admin.ts, sync-user.ts — undocumented migration scripts with no runner, no safety guards
These are orphaned scripts with no indication of whether they've been run, what state they expect the DB to be in, or whether they should be run again. They insert into the users table using clerkId as a conflict target. If run on a schema without clerkId, they crash. If run twice, results may be undefined.

4. db/migrations/ — conflicting migration files 0001_far_the_initiative.sql and 0001_multi_tenant.sql
Two files both claim to be migration 0001. The _journal.json cannot track both. This will cause drizzle-kit migrate to throw or silently skip one, leaving the DB in an inconsistent state depending on which environment the migration was run in.

5. app/api/leads/[id]/stage/route.ts — await req.json() is called without try/catch
Line: const { stage } = await req.json(). If the request body is not valid JSON, this throws an unhandled exception and returns a 500. All other similar routes have try/catch. This one doesn't.

6. app/api/leads/[id]/assign/route.ts — same missing try/catch on req.json()
const { assignedTo } = await req.json() has no error handling.

7. lib/app-user.ts — getProDbUser() is marked @deprecated but is still called from lib/tenant-access.ts
resolveTenantAccess calls syncAppUserFromClerk(clerkUserId) which is effectively the same function. The deprecated wrapper is not used directly, but the underlying pattern is still the only user-sync path.

8. @vercel/blob is a production dependency used for file uploads (leadUploadedDocuments.storageUrl)
Vercel Blob is a Vercel-specific service. It requires the BLOB_READ_WRITE_TOKEN env var which is Vercel-issued. File uploads will silently fail or throw on any non-Vercel host (including Replit's deployment environment) unless this is replaced with an S3-compatible storage solution.

9. lib/analytics-pipeline.ts — N+1 query pattern in AdminOverviewPage
Agent stats are loaded with Promise.all(proUsers.map(async (agent) => { db.select()... })). With 20 agents this fires 20 separate DB queries. Should be a single grouped query joining leads.assignedTo.

10. leadDocumentChecklist country-based checklist is deleted when the lead's country changes
In PATCH /api/leads/[id], when country changes, the entire checklist is deleted. There is no confirmation step, no backup, and no warning to the user. Documents marked as submitted are silently wiped.

11. app/t/[tenantSlug]/admin/team/page.tsx — imports clerkClient at the top but the import is unused in the visible portion
If the full file doesn't use it, this is a dead import that may cause TypeScript warnings or linter errors.

12. types/ directory — models.ts Tenant type includes clerkOrgId as a required field
Any code that constructs a Tenant object manually (e.g. in tests or mocks) must include clerkOrgId. When the migration eventually drops that column, type errors will appear across the codebase.

13. Missing environment variable usage without fallback: process.env.DATABASE_URL! with non-null assertion
In db/index.ts, the ! suppresses the TypeScript null check. If DATABASE_URL is undefined at runtime, Neon's client throws an opaque error rather than a clear startup message.

14. app/api/leads/import/route.ts — not audited in detail, but CSV imports have no row-level validation beyond the schema type check
Malformed phone numbers, emails, and names from CSV are inserted as-is. There is no sanitisation of Excel-style injection strings (values starting with =, +, -, @ that can trigger formula injection when the data is exported back to CSV).

15. roleRequests table uses requestedRole enum ['admin', 'pro'] but tenantMembers.role uses ['tenant_admin', 'agent']
These two enums are out of sync. When a role request is approved in admin/role-requests/[id]/route.ts, there is a manual mapping row.requestedRole === 'admin' ? 'org:admin' : 'org:member'. Any new role added to one enum must be manually mirrored in both the approval handler and the Clerk role map, creating a maintenance footgun.

[MISSING FEATURES]
Feature	HubSpot	Pipedrive	Zoho	Freshsales	This CRM
Custom pipeline stages	✓	✓	✓	✓	✗ (hardcoded enum)
Deal/revenue value on lead	✓	✓	✓	✓	✗ (no monetary field)
Lead scoring / AI ranking	✓	✓	✓	✓	✗
Two-way email sync	✓	✓	✓	✓	✗
Email templates + sequences	✓	✓	✓	✓	✗
WhatsApp / SMS integration	✓	✓ (via Zapier)	✓	✓	✗
Activity timeline on lead	✓	✓	✓	✓	Partial (type enum limited to: stage_change, note, call, message, document — but call/message are never populated)
Duplicate contact detection	✓	✓	✓	✓	✗
Bulk actions (delete, stage change, assign)	✓	✓	✓	✓	✗
Custom fields on leads	✓	✓	✓	✓	✗ (fixed schema)
Task management	✓	✓	✓	✓	Partial (reminders only, no task types)
Calendar / meeting scheduling	✓	✓	✓	✓	✗
Reporting (custom reports)	✓	✓	✓	✓	✗ (fixed dashboard only)
Revenue forecasting	✓	✓	✓	✓	✗
Automation workflows	✓	✓	✓	✓	✗
Web-to-lead forms	✓	✓	✓	✓	✗
Lead source attribution	✓	✓	✓	✓	Partial (source field exists, not tracked over time)
Mobile app	✓	✓	✓	✓	✗
API / webhooks	✓	✓	✓	✓	✗
Role permissions granularity	✓	✓	✓	✓	Partial (admin vs agent, no field-level control)
Agent performance reports	✓	✓	✓	✓	Partial (basic per-agent count table)
Notification centre	✓	✓	✓	✓	Partial (exists, in-app only, no email/push)
Multi-currency	✓	✓	✓	✓	✗
Document templates / e-sign	✓ (add-on)	✗	✓	✗	✗
Contact company association	✓	✓	✓	✓	✗ (no company entity)
Tags / labels on leads	✓	✓	✓	✓	✗
SLA / response time tracking	✓	✗	✓	✓	✗
Features specific to education/immigration consultancy that are missing:

Visa/application status tracking field (beyond the existing stages)
Country × programme preference tracking
Application deadline reminders tied to intake dates
Student visa document checklist is present but not customisable per country template at the tenant level
No GPA / score threshold automation for routing leads
No offer letter / conditional offer tracking
Fee payment tracking (a "paid" stage exists but no invoice amount or payment date field)
[UPGRADE ROADMAP]
Critical — Broken right now, blocking basic usage
#	What	Why it matters	Complexity
C1	Finish or abandon the auth migration — the codebase says it's migrating off Clerk but is still 100% on Clerk. Either complete the custom JWT system (auth.ts, sign-in pages, middleware JWT verification, remove ClerkProvider) or remove the migration intent entirely and keep Clerk. The current half-described state is a ticking bomb.	Every page and API route will break the moment Clerk keys are rotated or billing lapses	Large
C2	Fix the conflicting 0001 migration files — 0001_far_the_initiative.sql and 0001_multi_tenant.sql cannot both be tracked by the Drizzle journal. Run drizzle-kit status on a clean DB, pick one canonical migration history, delete the other, and re-seal the journal.	New environments and CI will have an inconsistent DB state	Small
C3	Replace @vercel/blob with an S3-compatible storage provider (Cloudflare R2, AWS S3, Supabase Storage)	Document uploads fail on any non-Vercel host. This is a core feature of the lead detail page	Medium
C4	Fix boolean-stored-as-text columns (required, isSubmitted on checklist; read on notifications) — run a migration to convert these to proper boolean columns and fix all query/comparison code	Silent bugs in document checklist and notification read-state	Small
C5	Add try/catch to req.json() in /api/leads/[id]/stage and /api/leads/[id]/assign	Malformed requests return 500 instead of 400, leaking stack traces	Small
C6	Fix the NEXT_PUBLIC_ROOT_DOMAIN invite redirect URL for Replit — update the env var to match the Replit .replit.app domain so Clerk invitation emails land users in the right place	Invited users click the email link and land on the wrong URL	Small
High Priority — Needed to sell this to 100 businesses
#	What	Why it matters	Complexity
H1	Custom pipeline stages — allow tenant admins to define, rename, reorder, and add stages via the UI. Store in a pipeline_stages table per tenant. Currently stages are a hardcoded TypeScript enum, meaning code must be redeployed to change them.	Every customer has a different sales process. Hardcoded stages are a dealbreaker for most buyers	Large
H2	Revenue / deal value on leads — add a numeric dealValue field (with currency) to the leads table. Aggregate this on the dashboard as pipeline value, won revenue, and conversion rate.	Without monetary data this is a contact tracker, not a CRM. Pipeline value is the #1 metric B2B customers want	Small (field) + Medium (dashboard)
H3	Bulk actions — checkboxes on the lead list table with actions: bulk assign, bulk stage change, bulk delete, bulk export.	Customers managing 500+ leads cannot work without bulk operations. This is day-one functionality	Medium
H4	Tags / labels on leads — a many-to-many lead_tags table, with a tag filter in the lead list.	Filtering by "UK Visa", "Masters", "Returning client" etc. is the primary way consultancies segment their pipeline	Medium
H5	Email notification on assignment and reminder — when a lead is assigned or a reminder is due, send an email (via Resend, already referenced in the codebase context). The notification table exists but only stores in-app records.	Agents miss assignments because they don't check the CRM. Email is the basic alert mechanism	Medium
H6	Fix the N+1 agent stats query in AdminOverviewPage — replace the Promise.all(proUsers.map(...)) loop with a single grouped SQL query	With 20+ agents, the overview page fires 20+ DB queries and becomes noticeably slow	Small
H7	Lead duplicate detection — on create and on import, check for duplicate phone number or email within the tenant and warn the user.	Immigration consultancies frequently get the same lead from multiple sources. Duplicates corrupt pipeline metrics	Medium
H8	Tenant-level user invitation from within the workspace (currently only platform super-admin can invite via inviteWorkspaceUserAction)	Tenant admins must be able to invite their own team without going through the platform admin panel	Medium
H9	Proper custom date range filter on reports — the analytics currently offers week/month/year windows. Customers need arbitrary date ranges (e.g. "last intake cycle" Sept–Dec)	Fixed windows are useless for seasonal businesses like education consultancies	Medium
Medium Priority — Increases retention and upsell
#	What	Why it matters	Complexity
M1	Web-to-lead form — a hosted, embeddable form that posts directly to /api/leads for a given tenant. Generate a unique form URL per tenant.	Stops manual CSV imports for new enquiries from the website	Medium
M2	WhatsApp integration (via 360Dialog or Twilio) — log WhatsApp messages against leads, trigger messages from the CRM	The primary communication channel for Pakistan/South Asia market this CRM appears to target	Large
M3	Custom fields per tenant — a lead_custom_fields definition table and lead_custom_values storage table. Show in the lead detail form.	Education consultants track IELTS scores, GPA, target country, intake date — none of these are in the schema	Large
M4	Lead source attribution + UTM tracking — extend the source field to include UTM parameters captured on the web-to-lead form. Report on which sources convert to paid.	Shows customers their marketing ROI	Medium
M5	Agent workload balancing — a round-robin or capacity-limit auto-assign feature. Each agent has a maxLeads setting; new imports auto-assign to the agent with fewest active leads	Prevents one agent being overloaded while others are idle	Medium
M6	Automated stage reminders — if a lead hasn't moved from follow_up in N days, automatically create an overdue reminder and optionally reassign	Prevents leads going cold silently	Medium
M7	Exportable reports — download pipeline by stage, agent performance, or date range as CSV/Excel directly from the dashboard	Every customer eventually asks for this for internal reporting to management	Small
M8	Delete confirmation with checklist impact warning — currently deleting a lead silently deletes all document checklist entries, reminders, and activities. Show a confirmation modal with a count of what will be lost.	Data loss without warning is a trust-breaker	Small
Nice to Have — Competitive differentiators
#	What	Why it matters	Complexity
N1	AI lead scoring — use OpenAI (env var already present: AI_INTEGRATIONS_OPENAI_API_KEY) to score lead conversion probability based on qualification, engagement, and stage history	Differentiates vs Zoho/HubSpot at a lower price point	Large
N2	Offer letter / conditional offer tracking — a structured sub-record on a lead for "university applied", "offer status", "conditions met" — specific to education consultancy	Direct competitor to niche products like UniAdvisor	Medium
N3	In-app Kanban board for agents (not just admin) — currently only admins have the kanban view	Agents do most of the pipeline moving. Giving them a Kanban view increases adoption	Small
N4	Audit log — every data change (who changed what field, when) stored and viewable by tenant admins	Required by enterprise buyers and regulated markets (visa processing)	Medium
N5	Two-way email sync via Gmail/Outlook OAuth — attach emails to leads automatically by matching sender/recipient	Removes the biggest friction in CRM adoption: manual data entry	Large
N6	Mobile-responsive PWA — the current UI uses lg:pl-52 sidebar offsets that collapse into a hamburger on mobile but the table-heavy lead lists are unusable on phone. A PWA wrapper with push notifications would serve agents in the field	Agents update stages from client meetings, not desks	Large
The most urgent single action is C1: decide whether to complete the custom auth migration or stay on Clerk. Everything else in the auth section flows from that decision. The current state — where the code claims to be migrating but all 20+ Clerk touchpoints are intact — means the next developer who touches auth will be confused about what's real. ] 

---

IMPLEMENTATION RULES:
- Work file by file. Show every complete file you change — no partial diffs.
- After each phase, tell me exactly what to test before moving to the next phase.
- If you need an env var that doesn't exist, tell me what to add to .env.local 
  and what value format it expects.
- Never delete working functionality while fixing something else.
- If two fixes conflict, stop and ask before proceeding.

---

PHASE 1 — CRITICAL FIXES (do these in order, do not skip ahead)

**P1-A: Resolve the auth direction (do this FIRST before touching any other file)**
Decision: KEEP CLERK. Do not migrate to custom JWT. 
Remove all references to a planned custom auth migration from comments and README.
Make sure NEXT_PUBLIC_ROOT_DOMAIN in .env.local matches the actual Replit deployment 
URL so Clerk invite emails redirect correctly.
Files to update: .env.local, any README or comments describing a migration away from Clerk.

**P1-B: Fix conflicting migration files**
Files: db/migrations/0001_far_the_initiative.sql, db/migrations/0001_multi_tenant.sql, 
db/migrations/_journal.json
Action: Determine which migration is canonical (check which tables actually exist in 
the DB), delete the other file, re-seal _journal.json so only one 0001 exists.
Show me the final _journal.json content.

**P1-C: Fix boolean-stored-as-text columns**
Files: db/schema.ts, then a new migration file
Columns to fix:
- leadDocumentChecklist.required (text → boolean, default false)
- leadDocumentChecklist.isSubmitted (text → boolean, default false)  
- notifications.read (text → boolean, default false)
Also update every file that reads or writes these columns to use true/false 
instead of 'true'/'false' strings. List every file changed.

**P1-D: Add try/catch to req.json() calls**
Files: 
- app/api/leads/[id]/stage/route.ts
- app/api/leads/[id]/assign/route.ts
Wrap req.json() in try/catch, return 400 with a clear message on parse failure.

**P1-E: Replace @vercel/blob with Cloudflare R2 (S3-compatible)**
The current file upload system uses @vercel/blob which only works on Vercel. 
This app runs on Replit.
Action:
1. Install @aws-sdk/client-s3 and @aws-sdk/s3-request-presigner
2. Create lib/storage.ts that exports uploadFile(buffer, filename, mimeType) 
   and getFileUrl(key) using S3-compatible env vars:
   R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, 
   R2_PUBLIC_URL
3. Replace all @vercel/blob calls with lib/storage.ts
4. Remove @vercel/blob from package.json
List every file changed.

**P1-F: Delete the dead dashboard test page**
File: app/dashboard/page.tsx
Action: Delete this file. It is not linked from anywhere and imports Clerk 
components for a test that was never cleaned up.

---

PHASE 2 — HIGH PRIORITY FEATURES (implement in order)

**P2-A: N+1 query fix in analytics**
File: lib/analytics-pipeline.ts (or wherever AdminOverviewPage loads agent stats)
Replace the Promise.all(proUsers.map(...)) loop with a single SQL query that 
groups leads by assignedTo and counts them. Show before and after query.

**P2-B: Revenue / deal value field on leads**
Steps:
1. Add dealValue (numeric, nullable) and dealCurrency (text, default 'USD') 
   to the leads table in db/schema.ts
2. Write and add the migration SQL file
3. Add the field to the lead creation form, lead edit form, and lead detail view
4. Add to the admin dashboard: total pipeline value (sum of all open leads), 
   won revenue (sum of leads in 'paid' stage), and conversion rate by count
5. Show every file changed

**P2-C: Tags / labels on leads**
Steps:
1. Create lead_tags table (id, tenantId, name, color) in db/schema.ts
2. Create lead_tag_assignments table (leadId, tagId) in db/schema.ts
3. Migration SQL
4. Tag selector component on lead detail page (multi-select, create new tag inline)
5. Tag filter chips on lead list page
6. Show every file changed

**P2-D: Bulk actions on lead list**
Add to the lead list table:
1. Checkbox column (select all + individual)
2. Bulk action toolbar that appears when ≥1 leads selected:
   - Bulk assign (dropdown of agents)
   - Bulk stage change (dropdown of stages)
   - Bulk export (CSV download of selected leads)
   - Bulk delete (with confirmation modal showing count)
3. All actions call new API endpoints:
   POST /api/leads/bulk-assign { leadIds, assignedTo }
   POST /api/leads/bulk-stage { leadIds, stage }
   POST /api/leads/bulk-delete { leadIds }
   POST /api/leads/bulk-export { leadIds } → returns CSV
4. Show every file changed

**P2-E: Email notifications via Resend**
Resend is already referenced in the codebase. Wire it up:
1. Create lib/email.ts with sendEmail(to, subject, html) using Resend
   Env var: RESEND_API_KEY
2. Trigger email in these events:
   - Lead assigned: email the assigned agent with lead name, phone, stage
   - Reminder due: email the agent with reminder text and lead name
   - Role request approved: email the user that they now have access
3. Keep all existing in-app notification logic — email is additive
4. Show every file changed

**P2-F: Lead duplicate detection**
On lead create and lead import:
1. Before inserting, query for existing leads in the same tenant with matching 
   phone OR email
2. On the create form: show an inline warning "A lead with this phone already 
   exists: [Name] — [Stage]" but allow override
3. On CSV import: add a "duplicates found" summary row in the import result, 
   with option to skip duplicates or import anyway
4. Show every file changed

**P2-G: Delete confirmation with impact warning**
When a user clicks delete on a lead:
1. Before deleting, query counts: activities, reminders, documents, checklist items
2. Show a modal: "This will permanently delete [Name] and their 4 activities, 
   2 reminders, 3 documents, and 6 checklist items. This cannot be undone."
3. Require typing the lead's name to confirm if counts > 0
4. Show every file changed

**P2-H: Custom date range filter on reports**
Replace or extend the fixed week/month/year dropdown in analytics:
1. Add a date range picker component (from/to date inputs)
2. Pass startDate and endDate as query params to all analytics API routes
3. All existing fixed windows stay as quick-select shortcuts
4. Show every file changed

---

PHASE 3 — MEDIUM PRIORITY (implement in order)

**P3-A: Exportable reports**
Add export buttons to the admin analytics/reports page:
1. "Export Pipeline CSV" — all leads grouped by stage with dealValue
2. "Export Agent Performance CSV" — per agent: leads assigned, leads won, 
   conversion rate, total deal value
3. Both respect the active date range filter from P2-H
4. Implement as /api/reports/pipeline-export and /api/reports/agent-export 
   returning CSV with Content-Disposition header
5. Show every file changed

**P3-B: Delete confirmation for checklist wipe on country change**
File: app/api/leads/[id]/route.ts (PATCH handler)
Currently when country changes, the entire document checklist is silently deleted.
Fix:
1. Before deleting, count how many checklist items exist and how many are submitted
2. If count > 0, return a 409 with body: 
   { requiresConfirmation: true, checklistCount: N, submittedCount: M }
3. The frontend must then show a modal warning and re-submit with 
   { confirmChecklistDelete: true } to proceed
4. Show every file changed

**P3-C: Fix dead import in team page**
File: app/t/[tenantSlug]/admin/team/page.tsx
Remove the unused clerkClient import at the top of the file.

**P3-D: Fix role enum mismatch**
The roleRequests table uses ['admin', 'pro'] but tenantMembers uses 
['tenant_admin', 'agent']. 
Fix:
1. Update roleRequests.requestedRole enum to ['tenant_admin', 'agent'] in schema
2. Update the approval handler in app/api/admin/role-requests/[id]/route.ts 
   to remove the manual Clerk role mapping and use the value directly
3. Update the request-role form UI to show the correct role options
4. Migration SQL
5. Show every file changed

**P3-E: Automated stage reminders (stale lead detection)**
Add a background job endpoint (callable via a cron):
1. POST /api/cron/stale-leads — protected by CRON_SECRET env var header check
2. Query all leads where stage = 'follow_up' AND updatedAt < now - 7 days 
   AND no open reminder exists
3. For each: create a reminder with text "Lead has been in Follow Up for 7+ days"
   and send an email to the assigned agent (using lib/email.ts from P2-E)
4. Set up the cron in replit's scheduler or document how to trigger it
5. Show every file changed

**P3-F: Web-to-lead public form**
1. Create app/form/[tenantSlug]/page.tsx — a public (no auth) page with a 
   lead capture form: name, email, phone, source, country, notes
2. On submit POST to /api/public/leads { tenantSlug, ...fields }
3. /api/public/leads — public route (no auth), looks up tenant by slug, 
   inserts lead with source='web_form', sends a notification to tenant admins
4. In tenant admin settings, show the form URL: 
   https://[domain]/form/[tenantSlug]
5. Show every file changed

---

PHASE 4 — EDUCATION CONSULTANCY SPECIFIC

**P4-A: Visa/application tracking fields**
Add to the leads table in db/schema.ts:
- targetCountry (text, nullable) — separate from existing country field
- targetProgramme (text, nullable)
- intakeMonth (text, nullable) e.g. "September 2025"
- intakeDeadline (date, nullable)
- ieltsScore (numeric, nullable)
- gpaScore (numeric, nullable)
- offerStatus (enum: null | 'applied' | 'conditional' | 'unconditional' | 'rejected')
- offerUniversity (text, nullable)
- feeAmount (numeric, nullable)
- feePaidDate (date, nullable)
- feeCurrency (text, default 'USD')
Migration SQL, add all fields to lead detail form and lead list filters.
Show every file changed.

**P4-B: Intake deadline reminders**
Using the intakeDeadline field from P4-A:
Add to the cron job from P3-E:
- 30 days before intakeDeadline: create reminder "Intake deadline in 30 days"
- 7 days before: create reminder "Intake deadline in 7 days — confirm documents"
- On the day: create reminder "Intake deadline TODAY"
All reminders emailed to assigned agent.

**P4-C: Agent Kanban view**
The Kanban board currently only exists for admins. 
Clone the admin Kanban view and expose it at:
app/t/[tenantSlug]/pro/kanban/page.tsx
Filter to only show leads assigned to the current agent (using session userId).
Add a "Kanban" link to the agent sidebar navigation.
Show every file changed.

---

AFTER ALL PHASES:
1. List every new env var introduced across all phases with example values
2. List every new npm package installed
3. List every new database table created
4. List every new API route created
5. Give me the final .env.local template with all variables

Do not start Phase 2 until I confirm Phase 1 is working.
Do not start Phase 3 until I confirm Phase 2 is working.
Do not start Phase 4 until I confirm Phase 3 is working.